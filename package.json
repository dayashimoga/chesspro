{
  "name": "chessos",
  "version": "1.0.0",
  "description": "Chess Mastery Platform — Zero to Grandmaster",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  },
  "dependencies": {
    "chess.js": "^1.0.0-beta.8"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
